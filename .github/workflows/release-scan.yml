name: Mend UA Release Scan

on:
  push:
    branches:
    - '**release**'

jobs:
  call-java-prioritize:
    uses: WFS-Mend/whitesource-config/.github/workflows/gradle_prioritize.yml@main
    with:
      gradle-cmd: 'gradle publish'
    secrets: inherit
